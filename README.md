# ArchSetup

Boot into live arch and run:
```
wget https://raw.githubusercontent.com/kubo11/archsetup/refs/heads/main/install.sh | /bin/sh
```

Reboot, remove installation media, log in and run:
```
wget https://raw.githubusercontent.com/kubo11/archsetup/refs/heads/main/postinstall.sh | /bin/sh
```